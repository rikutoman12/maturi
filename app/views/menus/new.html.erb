<%= form_with model: @menu, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>題名</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>
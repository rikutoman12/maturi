    <div class="text-center mt-3">
      <h2>一覧画面</h2>
    </div>
      <div class=container>
        <div class="row">
          <% @menus.each do |menu| %>
            <div class ="m-2">
              <div class="table-bordered" >
                <% if user_signed_in? %>
                  <%= link_to menu_path(menu) do %>
                    <%= attachment_image_tag menu, :image,:fill, 350, 300,fallback: "no_image.jpg",size:'350x300' %>
                    <div class="text-center">
                      <p><%= menu.title %></p>
                  <% end %>
                <% else %>
                  <%= link_to a_menu_path(menu) do %>
                    <%= attachment_image_tag menu, :image,:fill, 350, 300,fallback: "no_image.jpg",size:'350x300' %>
                      <div class="text-center">
                        <p><%= menu.title %></p>
                  <% end %>
                <% end %>
                    コメント数：<%= menu.comments.count %>
                    いいね数:<%= menu.likes.count %>
                  </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
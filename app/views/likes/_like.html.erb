 <% if menu.liked_by?(current_user) %>
        <p>
          <%= link_to menu_like_path(menu), method: :delete, remote: true do %>
            ♥<%= menu.likes.count %> いいね
          <% end %>
        </p>
        <% else %>
        <p>
            <%= link_to menu_likes_path(menu), method: :post, remote: true do %>
              ♡<%= menu.likes.count %> いいね
            <% end %>
          </p>
        <% end %>